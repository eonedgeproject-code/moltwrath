services:
  moltwrath:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  dashboard:
    build: .
    command: moltwrath dashboard
    depends_on:
      - moltwrath
